import CodexRun from '../CodexRun';

export default function S12_CodexRun2_DS() {
	return (
		<CodexRun
			no="②"
			tag="定品牌规范"
			minutes="6 min"
			title="让它把品牌变成一张能照着做的规范"
			sub="上一步写的那段还是草稿。这一步把它变成带颜色编号、带字号、带用法的单子，顺便出一个能打开看的样式页。"
			prompt={`读我的 event-sot.md，把里面的 brand 段扩成一份完整的 design system，
输出两个文件：

1. design-system.md — 规范本体
   - 色板：主色 / 辅色 / 中性色，每个给 hex + 用在哪
   - 字体：标题字体 + 正文字体，各自字号阶梯（H1/H2/正文/说明）
   - 间距与圆角：给具体数值
   - 语气：三个形容词 + 三个禁用词，各配一个正例一个反例
   - 图片风格：一句话 + 三个关键词（之后出图直接用）

2. design-system.html — 一页能打开看的样式预览
   - 把上面的色板、字号阶梯、按钮、卡片都画出来
   - 单文件，内联 CSS，不引外部资源

规则：
- 颜色必须给 hex，不许写"暖橙色"。
- 每条规范后面写一句"为什么这么定"，一句话。
- 不要写"赋能""一站式""全方位"。`}
			accept={[
				'色板每个色都有 hex，且说了用在哪',
				'字号是一个阶梯（H1/H2/正文/说明），不是一个数',
				'禁用词是具体的词，不是"避免空话"这种空话',
				'html 打开能看到色块和字号，不是白页',
			]}
			fallback="html 打不开就先看 md，色板念出来也行。这一步的产出是「参数」，不是「好看」——参数对了就往下走。"
		/>
	);
}
