# Vibe Coding 大师课 · 第八节蓝图

> 状态：DRAFT 0.1（2026-08-02 · 从 L7 v0.4 拆分而来）
> 主题：**Agent Team —— 从分派到协作**
> 课型：**120 分钟动手工作坊**（一次协作实验 + 老师投屏演示）
> 本文只定义课程，不包含 deck 实现。
> 承接第七节：L7 教学员给 context 分家——把脏活派出去，只收结论。但那些分出去的 context **互相不说话**。L8 回答下一层问题：**什么时候它们必须说话，以及说话之后谁来收敛。**

## 0. 本节的来历

本节内容原本是 L7「双结构版」（v0.3/v0.4）的后半段。做 deck 时发现三个结构性问题，遂拆课：

1. **两侧无法对称**——Team 侧天然多一层（charter、成员通信、对抗设计、外部验收），塞在同一节里会让学员误以为 Team 更重要，**而核心判断恰恰相反**。
2. **120 分钟装不下两次实验 + 两套开课门槛**——Agent Team 额外需要实验开关、显示模式、权限预批三项预检。
3. **判断线被迫前置**——双结构版必须在实验前讲完决策树，违反系列一贯的「先撞墙，再给尺子」。

拆开后，L7 专注 Subagent 并恢复动手红灯；本节专注协作，并且**有足够时间处理 Agent Team 那些真实的开课门槛**（§12）。

原 L7 deck 中属于本节的页已归档在 `lessons/vibe-coding-master-l7/src/components/slides/_archive/`，可直接迁移（见 §11 复用列）。

## 1. 课程定位

第七节结束时，学员会做一件事：**把一个任务拆成几路，派出去，收回来自己汇总。**

但那个结构有一个硬边界，L7 只埋了一句没有展开：

> **子 Agent 之间没有连线。** A 发现的东西，B 永远不知道——除非经过你。

大部分时候这不是问题。但有一类任务，这个限制会直接毁掉结论质量：

```text
三个人各查一个假设 → 各写各的报告 → 你拿到三份互不冲突的文档
                                      ↓
                    每份看起来都说得通，因为没人去挑战别人
                                      ↓
                    你还是不知道哪个是真的
```

第八节换结构：**让它们边做边互通。**

```text
L7：Hub-and-spoke
    你 ↔ 主 Agent ├→ A ─┐
                  ├→ B ─┼→ 只回结论 → 你汇总
                  └→ C ─┘
    成员之间没有连线。分工 = 覆盖。
                  ↓
L8：Team
    Team Lead ↔ A ↔ B ↔ C  +  共享任务板 / 信箱
    成员互相挑战、交换证据、协调依赖。对抗 = 收敛。
                  ↓
    但天下依然没有白拿的：
    通信成本、任务状态、冲突处理、更高总消耗
    所以核心不是「更高级的结构」，是「这件事需不需要它们说话」
```

一句话定位：

> 第七节教你**把活分出去**；第八节教你**判断什么时候分出去的人必须互相说话**——以及说完之后，谁负责收敛。

系列主线走第四步：

```text
L1–L5：往 context 里放对的东西
L6：  看懂 context 怎么被消耗、怎么坏
L7：  给 context 分家
L8：  让分出去的 context 互相通信
```

## 2. 两种结构对照

学员已经用透了左边这一列，所以这张表现在才讲得动——**对比需要基础**。

| | Subagent（L7） | Agent Team（本节） |
|---|---|---|
| 形状 | **Hub-and-spoke（主从式）** | **Team（团队式）** |
| 谁分派 | 主 Agent | Team Lead；成员也可能领取任务 |
| 成员通信 | 通常只把结果回给主 Agent | 成员可持续共享发现、互相质疑、协调依赖 |
| 协调发生在哪 | 主 Agent 集中拆分与汇总 | Lead + 任务系统 + 成员通信共同协调 |
| 生命周期 | 聚焦、一次性、完成即返回 | 更持续，任务会更新、阻塞、移交或重分配 |
| 写入隔离 | **可以**给每个成员一份独立工作副本 | **不提供**，成员共用工作副本，只能靠划文件所有权 |
| 嵌套 | 允许（有层数上限） | 不允许，成员不能再开成员 |
| 异构 | **可以**（L7 §18.7 的两条路） | **不行**，成员资格靠共享任务板和信箱 |
| 最适合 | 大量独立搜索、日志分析、明确验证、互不依赖的调查 | 竞争假设、跨模块协作、接口协商、需要成员互相挑战的复杂任务 |
| 主要成本 | 冷启动、brief、回报与汇总 | Subagent 的**全部成本** + 消息、任务状态、冲突处理和更高总消耗 |

一句话判断线：

> **只需要「分头做、回来报」用 Subagent；需要「边做边互通、共同收敛」才用 Agent Team。**

Agent Team 不是更高级、更强的 Subagent。任务不需要成员通信时，上 Team 只会多一层协调成本。

## 3. 学员起点

学员上课前应有：

- **L7 的完整体感**：会写六格 brief、跑过三路并行调查、做过一次汇总矩阵、**亲手测过一次过度分派的代价**。没有最后这一条，学员会把 Team 当成「更强的 Subagent」，这是本节最需要防的偏差。
- L7 的三条铁律（不假设继承 / 不重叠所有权 / 不拿自述验收）。
- L6 的诊断体感。
- 本机有一种支持成员消息与共享任务板的编码 Agent；**若不支持，见 §15 的人肉信使降级方案**（这不是备选，对国内学员是主路径）。
- 能打开老师提供的**统一兜底仓库**——与 L7 同一个，省掉重新熟悉的时间。

### 3.1 课前能力预检（老师完成，学员不现场折腾配置）

比 L7 的预检卡多三行，而且这三行都是**跑不跑得起来**的开关：

| 能力 | 当天实测结果 | 课堂影响 |
|---|---|---|
| **协作结构是否要显式开启** | 开箱可用 / 要开开关 / 不支持 | **不开就没有本节的实验** |
| **怎么确认结构真的成型了** | 实测动作：______ | 防止 Subagent 冒充 Team |
| **成员消息看不看得见** | 单终端 / 分屏 / 看不见 | 决定投屏演示长什么样 |
| 成员是否继承 Lead 的对话历史 | 无 / 摘要 / 其他 | spawn prompt 要补多少 context |
| 成员默认用哪一档模型 | 跟随 Lead / 不跟随 / 其他 | 见 §6.8 |
| 多个成员使用同一还是隔离工作副本 | 同目录 / 隔离 | 能否安全并行写 |
| 权限弹窗出现在哪 | Lead / 各自 / 无 | 要不要课前预批 |
| 是否允许嵌套 | 是 / 否 | demo 的最大深度 |

前三行是 v0.1 相对 L7 新增的，因为它们不是「了解一下」，而是**开课门槛**。具体到当天工具怎么开、怎么验，见 **§18**。

## 4. 学习目标

课程结束后，学员能够：

1. 画出 Subagent 与 Agent Team 两张结构图，并从**通信路径、协调位置、生命周期、成本**四个维度解释差别。
2. 用**第二问**判断一件事该用 Subagent 还是 Agent Team：*这些 context 之间需不需要持续通信和共同协调？*
3. 说清 **Team 的门槛必须比 Subagent 高**：只有当**成员通信会改变任务分工或结论质量**时它才值得。
4. 区分**对抗**与**分工**——并说清为什么按模块分工的「团队」其实是 Subagent，只是更贵。
5. 说清对抗辩论治的是什么病（**锚定**：单个 Agent 找到一个说得通的解释就停下来了）。
6. 为 Agent Team 写一份 **Team charter**：共同目标、成员角色、任务依赖、通信规则、写入边界、收敛条件。
7. 认出并记录三类关键消息：**DISCOVERY / CONFLICT / DECISION**，并说清为什么只留这三类。
8. 说清 **Team Lead 的收敛责任**：成员「已经达成一致」不是外部判据；任务板完成 ≠ 外部验收完成。
9. 认出**过度组队**：一个不需要成员通信的任务被组成团队，结果只是多付一层协调成本。
10. 说清 **异构只能进 Subagent，进不了 Agent Team**，以及为什么（回收 L7 §18.7）。

### 4.1 过关证据

不是「成功开出了一支队伍」，而是学员离场前能提交：

- 一份 **Team charter**（六项填全）。
- 一份实验的**协作包**：任务板快照 + 至少各一条 DISCOVERY / CONFLICT / DECISION 消息 + Lead 最终结论。
- 一张**结构选择卡**：这个任务该用哪种结构 + 一句理由。
- 一句话说明：**如果禁止成员互相通信，这个任务会在哪一步变慢或变差。** 答不出来 = 它本来就不该用 Team。

## 5. 非目标

本节明确不做：

- **不重讲 Subagent 的基本功**（六格 brief、汇总矩阵、完成回执格式）——那是 L7 的内容，本节只回收不展开。
- 不教 SDK / API，也不搭多 Agent 编排框架。
- 不教具体工具的配置项、模型参数或并发上限。**唯一例外**：协作结构的开关必须讲——它是这节课跑不跑得起来的前提（§18）。
- 不把「同时启动很多 Agent」当学习成果；课堂并行上限固定为 **3 个成员**（Lead 不计入）。
- 不让学员在课堂里并行改同一份真实代码；实验以只读为主，写冲突由老师演示。
- 只讲**最小协作闭环**：共同目标、任务板、成员消息、收敛与验收；不展开消息协议、失败恢复和跨机器协作。
- 不声称所有工具都提供成员通信或共享任务板——**大多数不提供**，见 §15。

## 6. 核心教学决策

### 6.1 全节只回答第二问

L7 回答了第一问（值得开多个 context 吗）。本节回答第二问：

> **这些 context 之间需要持续通信吗？**

```text
只需把结果交回一个中心 → Subagent（L7 已经会了）
需要互相交换发现、处理依赖、挑战结论 → Agent Team（本节）
```

任何内容如果不能帮助学员完成这次判断、运行一个 Team 或验收结果，就移出主课。

### 6.2 不是能力等级，是通信拓扑

错误心智：

```text
单 Agent < Subagent < Agent Team
Agent 越多、结构越复杂就越强
```

正确心智：

```text
任务的通信需求
   ├─ 无额外 context 收益 → 单 Agent
   ├─ 独立分工，集中汇总 → Subagent
   └─ 持续互通，共同收敛 → Agent Team
```

**选择 Agent Team 的门槛必须比 Subagent 高。** 这一条要反复说，因为本节最容易造成的伤害是「学完什么都组队」——就像 L7 最容易造成「学完什么都派」。

### 6.3 对抗辩论是 Team 唯一不可替代的场景

这是本节的立论页。

**它治的是一种具体的病：锚定。**

```text
单个 Agent 查根因：
  试假设 1 → 说得通 → 停
  假设 2、3 从此再没被认真查过

三个 Agent 互相证伪：
  假设 1 ↔ 假设 2 ↔ 假设 3
  各自被另外两个攻击 → 活下来的那个
```

顺序调查有个固有毛病：**第一个说得通的解释会污染后面所有判断**。辩论不是为了热闹，是为了**不让第一个答案自动胜出**。

### 6.4 对抗 ≠ 分工（本节最容易做错的一步）

| | 分工 ❌ | 对抗 ✅ |
|---|---|---|
| 怎么派 | A 查前端 · B 查后端 · C 查配置 | A、B 各认领一个假设 · C 专职找反例 |
| 拿回来 | 三份互不冲突的报告 | 一个被证伪不掉的结论 |
| 拿到的是 | 覆盖 | **收敛** |
| 实质 | 没人有动机推翻别人——**这是 Subagent，只是更贵** | 每个人都有动机推翻别人，活下来的才算数 |

**必须有人负责唱反调。** 如果三个成员都在「证明自己的假设」，没人在「推翻」，辩论就退化成三份并行报告。这就是实验里 C 角色（Evidence challenger）存在的全部理由。

### 6.5 什么时候辩论才值

> 当这件事**没有廉价判据**的时候。

跑个测试就能证伪的假设，直接跑测试——别开三个 Agent 辩论。辩论换的是「**拿不到确定判据时，怎么让结论更可信**」。

这条是 §6.2 门槛的操作化版本，也是红灯判断的依据。

### 6.6 冲突靠证据裁决，不靠投票

对抗辩论最容易滑坡的地方：三个 Agent 很容易「达成一致」——**而一致不等于对**。

所以 charter 的通信规则必须写死：与其他结论冲突时，**附双方证据，不靠投票解决**。Lead 按证据裁决。

### 6.7 收敛：任务板完成 ≠ 外部验收完成

成员可以在团队内交换证据、暴露矛盾、调整任务，但 **Team Lead 仍要做最终收敛与验收**。

> **成员「已经达成一致」不是外部判据。**

全绿之后 Lead 还要问：

1. 根因结论追溯得到文件 / 行号 / 命令输出吗？
2. 另外两个假设的**排除**有反证，还是只是「没查到」？
3. 验收判据是不是全部真的执行过？
4. 成员没检查的范围，标出来了吗？

一句话收口（与 L7 同一句，本节加重）：

> **多 Agent 改变的是信息怎样流动，不会转移最终验收责任。**

### 6.8 成员选档：跟 L7 同一条判断线，但多一个陷阱

判断线不变——**跟判断密度走，不跟重要性走**：

| 角色 | 密度 | 选档 |
|---|---|---|
| **Team Lead** | 判断密集（读矛盾证据然后拍板） | 最强档。这里省钱最亏 |
| **Evidence challenger** | 判断密集（找反例是纯判断） | 最强档 |
| Client / API investigator | 执行密集 | 中间档够用 |

新增的陷阱（L7 没有）：

> **成员默认不跟随 Lead 的模型选择**，而且**开人时就定死，事后改不了**。
>
> 这跟 subagent 正好相反（subagent 默认 `inherit`）。又一次「不假设继承」，而且方向反直觉。

### 6.9 异构：回收 L7 的那句埋点

L7 §18.7 埋了一句：异构只能进 Subagent。这里回收并讲透：

**成员资格靠共享任务板 + 信箱**，那是工具内部的东西。外部进程收不到消息、不出现在任务板、Lead 看不见它的进度——它永远只能是 Hub-and-spoke 的一根 spoke，**做不了队友**。

推论：想混用别家的 Agent，只能停在 L7 那种结构。这不是产品缺陷，是**结构决定的**。

## 7. 课堂最终产物

每位完成者应有：

- 一张**能力预检卡**（老师当天实测版，含三行开课门槛）。
- 一张**结构选择卡**：单 Agent / Subagent / Agent Team + 理由。
- 一份 **Team charter**（六项）。
- 一份**协作包**：任务板快照、三类关键消息各至少一条、Lead 最终结论。
- 一张**两种结构对照表**，能标出通信路径、协调位置与主要成本。
- 一句话：如果禁止成员互相通信，这个任务会在哪一步变慢或变差。

## 8. 120 分钟流程

| 时间 | 阶段 | 课堂动作 | 阶段产物 |
|---|---|---|---|
| 0–10 min | 开场：L7 那条没讲的边界 | 回放「成员之间没有连线」；三份互不冲突的报告有什么问题 | 「该让它们说话」的共识 |
| 10–22 min | 两种结构对照 | 两张拓扑图同屏；四维 + 写入隔离 + 异构对照 | 双结构心智 |
| 22–32 min | 第二问 + 门槛 | 通信分叉；Team 门槛必须更高；6 张任务卡分类 | 结构选择卡 v1 |
| 32–40 min | 能力预检 + 选档 | 三行开课门槛当场投；成员选档的反直觉陷阱 | 预检卡 |
| 40–52 min | **对抗辩论：立论页** | 锚定效应；**对抗 ≠ 分工**；什么时候辩论才值 | 对抗设计判断 |
| 52–64 min | Team charter | 六项；通信规则与收敛条件写死 | charter |
| 64–69 min | 休息 | — | — |
| 69–92 min | **动手：三个竞争假设** | 成员交换证据、挑战矛盾、更新任务板 | 协作包 |
| 92–102 min | 讲评：价值发生在消息里 | DISCOVERY / CONFLICT / DECISION；一条 CONFLICT 都没有说明什么 | 关键消息记录 |
| 102–110 min | 收口 + 红灯 | 任务板全绿 ≠ 验收完成；微任务识别过度组队 | Lead 验收清单 |
| 110–115 min | 异构与结构边界 | 异构只能进 Subagent；回收 L7 埋点 | 边界认知 |
| 115–120 min | Exit ticket + 作业 | 画图、选结构、写理由 | 过关证据 |

> **节奏铁律**：对抗辩论立论页（40–52）和实验（69–92）是本节的两根支柱，都不可砍。时间不足时依次砍：异构段（110–115）→ 红灯 → charter 逐项展开。**不能把对抗辩论压缩成实验前的一句嘱咐**——那样实验必然退化成分工。

## 9. 各阶段教学细节

### 9.1 开场：L7 那条没讲的边界

先投 L7 的 Hub-and-spoke 图，把那句话再说一遍：**成员之间没有连线。**

然后给一个具体场景：

> 登录偶发 401。你按 L7 的方法派了三路：查客户端缓存、查 token 轮换、查测试环境配置。
>
> 三份报告回来了，每一份都说「我这边看起来没问题，但不排除是别处」。
>
> **你还是不知道根因。**

问一句：**问题出在哪？** 引出——没有人去挑战别人的结论，所以三个「看起来没问题」谁也没被证伪。

### 9.2 两张拓扑图同屏

```text
【Subagent：Hub-and-spoke】

                    ┌→ 子 Agent A ─┐
你 ↔ 主 Agent / Hub ├→ 子 Agent B ─┼→ 结果回 Hub → Hub 汇总
                    └→ 子 Agent C ─┘

成员之间没有连线；分工、补 context、冲突处理都经过 Hub。

【Agent Team：Team collaboration】

                   ↔ Teammate A ↔
你 ↔ Team Lead     ↕      ↕      ↔ 共享任务板 / 信箱
                   ↔ Teammate B ↔

成员不仅交结果，还会互通发现、处理依赖、挑战彼此结论。
```

立刻问：如果 A、B、C 完全不需要说话，为什么要付 Team 的通信成本？

### 9.3 快速分类

给 6 张任务卡，让学员投「Subagent / Agent Team」：

1. 从 20,000 行日志里找首次异常并给证据。
2. 分别排查前端、API、数据库三种登录失败假设，且**根因未知**。
3. 前后端要持续协调接口变化并分别实现。
4. 对已完成的权限改动按 5 条安全判据做只读审查。
5. 把三个模块的 README 各更新一遍。
6. 一个设计选型，三个方案各有取舍，需要有人反对。

第 5 题是陷阱：三个模块互不依赖 → **分工，不是对抗** → Subagent。

### 9.4 对抗辩论立论页

按 §6.3、§6.4、§6.5 讲。三块内容顺序不能换：**先讲锚定（为什么要对抗），再讲对抗 ≠ 分工（怎么设计），最后讲什么时候才值（门槛）。**

三个失败模式当场点出：

| 失败模式 | 症状 |
|---|---|
| **退化成分工** | 三个人各写各的，一条 CONFLICT 都没有——最常见 |
| **虚假共识** | 都同意了，但都错。「达成一致」不是外部判据 |
| **Lead 抢活** | Lead 自己下场做，没人裁决，辩论没发生 |

### 9.5 Team charter

开工前至少定义六项：

```markdown
# Team charter

## 1. 共同目标
整个 Team 最终要交付或回答什么：______

## 2. 成员与初始所有权
- Lead：拆分、盯覆盖、处理冲突、最终验收
- Teammate A：______
- Teammate B：______
- Teammate C（唱反调）：______

## 3. 初始任务与依赖
| Task | Owner | Depends on | Done when |
|---|---|---|---|
|  |  |  |  |

## 4. 通信规则
- 发现会改变别人方向的证据：立即发给相关成员并抄送 Lead。
- 与其他结论冲突：**附双方证据，不靠投票解决**。
- 阻塞超过 ______：更新任务状态并请求重分配。

## 5. 写入边界
- 每位成员独占：______
- 共同文件由谁集成：______

## 6. 收敛与停止条件
- 所有必选任务完成；关键冲突有证据裁决；验收判据全部执行。
```

核心差异（回收 L7）：**L7 的六格 brief 只需让「一个 Agent 独立做完」；charter 还必须定义 Agent 和 Agent 之间怎样协作。**

⚠️ 通信规则里没有广播——**成员之间发消息要一个个点名**，想让所有人知道一件事得发多条。所以「会改变别人方向的证据要抄送谁」必须在 charter 里写死，不能靠临场判断。

### 9.6 动手：三个竞争假设

统一问题示例：

> 「登录偶发 401 的根因是什么？当前有客户端缓存、API token 轮换、测试环境配置三种竞争假设。找出根因，并排除另外两种。」

三个初始角色：

- A · Client investigator：检查客户端缓存与刷新时序。
- B · API investigator：检查服务端 token 轮换与错误路径。
- C · Evidence challenger：检查配置 / 测试复现，并**主动寻找能推翻 A、B 的证据**。

课堂脚本必须制造至少一次**通信有价值的时刻**：

1. A 发现一个时间戳，可能改变 B 的排查范围，必须发消息给 B。
2. B 的初步结论与 C 的复现结果冲突，双方必须附证据处理，而不是各自交报告。
3. Lead 根据新证据更新任务板：关闭一个假设、重派一个验证任务。
4. Team 最终输出根因、排除证据、未解决风险和可执行验收。

**计时终点**：不是成员都标记 done，而是冲突已处理、根因有证据、排除项有反证、**Lead 完成外部验收**。

### 9.7 讲评：价值发生在消息里

关键消息记录只保留三类：

```markdown
[DISCOVERY] 新证据是什么，它影响谁：______
[CONFLICT]  哪两条结论冲突，各自证据：______
[DECISION]  Team 如何裁决，谁继续做什么：______
```

为什么只留三类：**只有这三类会改变后续行动**，其它都是噪音。

讲评只问一句：**如果禁止成员互相通信，这个任务会在哪一步变慢或变差？** 答不出来，说明它本来就不值得用 Agent Team。

配一句诊断：**一条 CONFLICT 都没有的 Team，大概率是三个不说话的 Subagent。**

### 9.8 过度组队红灯（5 分钟判断）

给一个微任务，让学员分别估算单 Agent、Subagent、Agent Team 的启动与协调动作。

> L7 的红灯是**动手实测**（因为「派不派」是那节的核心判断）；本节的红灯是**判断题**，因为学员已经在 L7 亲手测过分派成本，Team 的额外成本可以在那个基线上推算。

过关答案：它没有多 context 强收益，**更没有成员通信需求**，应该直接做。

**能组 Team，不代表该组 Team。**

### 9.9 收口：Lead 的验收清单

按 §6.7 走。配一份「任务板全绿但不可验收」的反例：所有任务 completed，但根因结论没有行号、排除项只写「没查到」、验收命令没跑过。

## 10. 结构选择卡（完整版）

本节把 L7 的分派判断卡补成完整两问版：

```markdown
# 多 Agent 结构选择卡

## 任务
______

## 第一问：值得开多个 context 吗？（L7 已学）
### 强收益（至少命中一项）
- [ ] 会产生大量我不需要留在主 context 的噪音
- [ ] 有多个互不依赖、可以同时推进的分支
- [ ] 需要一个按明确判据工作的独立视角
- [ ] 多个角色必须交换证据、协调依赖或共同处理冲突

### 就绪条件（必须全过）
- [ ] 边界能一句话说清
- [ ] 必要 context 能一次给齐或指向文件
- [ ] 产出格式和完成判据明确
- [ ] 没有未处理的写入重叠或前后依赖

若没有强收益：选单 Agent，停止判断。

## 第二问：成员之间需要持续通信吗？（本节）
- [ ] 每个任务可独立完成，只需把结果交回中心
- [ ] 一个成员的新发现可能改变另一个成员的下一步
- [ ] 存在需要成员共同处理的依赖、冲突或竞争假设
- [ ] 任务会持续更新、阻塞、移交或重新分配

只有第一项 → **Subagent**。
后面三项命中多项，且通信价值覆盖成本 → **Agent Team**。

## 如果选了 Team，再问一句
- [ ] 这件事有没有廉价判据？（跑个测试就能证伪 → 别辩论，去跑测试）
- [ ] 我设计的是**对抗**还是**分工**？（按模块切 = 分工 = Subagent 更便宜）
- [ ] 谁负责唱反调？______

## 选择
- [ ] 单 Agent
- [ ] Subagent（主从式）
- [ ] Agent Team（团队式）

一句话理由：______
```

## 11. Slide 蓝图（建议 18 页）

> 「复用」列指向 `lessons/vibe-coding-master-l7/src/components/slides/_archive/` 里已写好的页——直接迁移到 L8 deck 目录并改前缀即可。

| 页 | 标题 | 页面任务 | 复用 |
|---|---|---|---|
| P00 | Agent Team · 从分派到协作 | 封面 | 🆕 |
| P01 | L7 那条没讲的边界 | 成员之间没有连线；三份「看起来没问题」 | 🆕 |
| P02 | 同样是多 Agent，信息走法不同 | 两张拓扑图同屏 | `L7P03_TwoTopologies` |
| P03 | 结构 B：Agent Team | Team；任务状态、成员通信、共同收敛 | `L7P05_StructureB` |
| P04 | 不是强弱，是通信需求不同 | 对照表（含写入隔离 / 嵌套 / 异构） | `L7P06_NotStrongerJustDifferent` |
| P05 | 第二问 | 通信分叉；Team 门槛必须更高 | 拆 `L7P07_TwoQuestionTree`（右半） |
| P06 | 今天这套工具真正支持什么 | 能力预检卡（三行开课门槛） | 改 `L7P08_CapabilityCheck` |
| P07 | 成员用什么档 | 判断密度 + 「成员默认不跟随 Lead」陷阱 | 改 `L7P08b_ModelChoice` |
| P08 | **对抗辩论：为什么要让它们吵** | **立论页**：锚定 + 对抗 ≠ 分工 + 三个失败模式 | `L7P12c_AdversarialDebate` |
| P09 | 先写 Team charter | 六项；通信规则与收敛条件 | `L7P12_TeamCharter` |
| P10 | 你实际要打什么字 | spawn prompt + 「互相推翻彼此」那半句 + 怎么确认成型 | `L7P12b_HowToSpawnTeam` |
| P11 | 动手：三个竞争假设 | 角色 + 课堂脚本 + 计时终点 | `L7P13_LabB` |
| P12 | 价值发生在消息里 | DISCOVERY / CONFLICT / DECISION | `L7P14_ValueInMessages` |
| P13 | 任务板完成 ≠ 外部验收完成 | Lead 的四个追问 | `L7P15_TeamClosing` |
| P14 | 红灯：能组 Team，不等于该组 | 微任务；三结构成本对照 | 改 `L7P16_RedLight` |
| P15 | 写入所有权 | Team 不做副本隔离，只能靠划文件 | 改 `L7P17_ContextVsWorkspace` |
| P16 | 异构只能进 Subagent | 回收 L7 埋点；为什么做不了队友 | 改 `L7P15b_Heterogeneous` |
| P17 | 收尾 | 三选一 + Exit + 作业 + 预告 | 改 `L7P19_ExitAndNext` |

## 12. 老师课前准备

### 12.1 硬要求

- 🔴 **按 §18 把 Agent Team 真的开一遍并跑通**，确认开关已开、team 真的成型（不是 Subagent 冒充）。这是所有其它准备的前提，没跑通别往下做。
- 🔴 **定好投屏用的显示模式**。默认模式下成员之间的消息发生在你看不到的地方，而本节的教学目标就是让学员**看见**消息在流动。上课那台机器、那个终端能不能开分屏，课前必须试出来。不能的话，预录从「降级方案」升格为**主要素材**。
- 🔴 **课前预批常用只读操作**。成员的权限弹窗会全部冒到 Lead 那一个屏幕上，实验只有 23 分钟，不预批就会全花在点确认上。
- 实验题必须含**一个跨成员新证据、一个结论冲突、一次任务重分配**——缺这三项就展示不出 Team 的价值。
- 标准任务板、三条关键消息和最终证据链，供课堂运行失败时降级投屏。
- 一份「任务板全绿但不可验收」的反例。
- 若主工具不支持成员消息或共享任务板，准备一段支持 Agent Team 的官方工具预录；**不要把多个互不通信的 Subagent 冒充 Team**。
- 打印或电子版：结构选择卡、Team charter、任务板、关键消息卡。

### 12.2 课前彩排验收

- [ ] 协作开关已开，**新开一个会话确认**（不是靠记忆）。
- [ ] 真的开出一个 team，成员名单里有多于一个成员。
- [ ] 让两个成员互发一条消息并在 Lead 侧看到——**这是 team 与 subagent 的唯一硬区分**。
- [ ] 投屏机器上确认显示模式；不能分屏就把预录升为主素材。
- [ ] **实验题若禁止成员通信，完成质量或速度会明显下降**；否则重做题目。
- [ ] 关键证据能稳定触发 discovery → conflict → decision 三类消息。
- [ ] Team Lead 不会把所有工作抢回自己做，成员也不会只发最终报告。
- [ ] 所有任务默认只读；即使全班同时运行也不会污染仓库。

## 13. 高频风险与课堂降级

| 风险 | 处理 |
|---|---|
| **学员把 Team 当 Subagent 的别名** | P02–P05 强制画两张结构图；每个任务都追问「成员能否互相通信」 |
| **工具用 Subagent 冒充 Team** | 开跑后立刻验成员名单（§18）；这是本节头号坑 |
| 协作开关没开 | 不报错、静默无效——课前彩排第一条就是验这个 |
| 实验成员各做各的、没有通信 | 立即插入预设冲突证据，要求相关成员互发消息并更新任务板 |
| **实验退化成分工** | 说明 P08 立论页没讲透；当场重申「对抗 ≠ 分工」并重派 C 角色 |
| Team Lead 把任务全部抢回自己做 | 重申 Lead 负责覆盖与收敛，不重做成员调查；按所有权退回任务 |
| 权限弹窗淹没 Lead 屏幕 | 课前预批；现场只能临时放宽或改用预录 |
| 成员消息看不见 | 切分屏；不支持就投预录，学员用纸面消息卡模拟 |
| 工具完全不支持 Team | 走 §15 人肉信使方案——**对国内学员这是主路径不是降级** |
| 报告都很漂亮但没有证据 | Lead 验收清单强制 evidence、排除反证、not_checked |
| 时间不够 | 保 P02–P05、P08、P11–P13；P15–P16 压成老师讲评 |

## 14. Exit ticket 与作业

### 14.1 Exit ticket（5 分钟，四题）

1. 不看笔记，画出 Subagent 和 Agent Team 的通信结构。
2. 「A 查前端、B 查后端、C 查配置」——这是对抗还是分工？该用哪种结构？
3. Team 全部任务显示 completed，是否代表可以验收？还缺什么？
4. 为什么异构（混用别家的 Agent）只能进 Subagent？

过关标准：四题至少三题正确，且第 2 题必须答对。

### 14.2 作业

1. 在真实项目里找一个**需要成员通信**的任务，先填结构选择卡；不能先开队伍后补理由。
2. 提交 Team charter、任务板快照、至少一条有价值的 DISCOVERY / CONFLICT / DECISION 消息、Lead 最终结论。
3. 至少执行一个来自成员自述之外的**验收动作**，并附输出。
4. 再找一个**看起来像团队协作、其实是分工**的任务，说明为什么 Subagent 更划算。
5. 选做：把同一个任务分别用 Subagent 和 Agent Team 各跑一次，记录时间、消息数、结论差异。

提交包：结构选择卡 + 协作包 + 验收证据 + 150 字复盘。

## 15. 国内环境：没有 Team 结构怎么上这节课

> **这一节对国内学员不是降级方案，是主路径。** L7 的 Subagent 国内工具普遍都有；本节的 Agent Team 大部分拿不到。

先把话说在前面：**本节的判断框架是工具无关的，产品实现不是。** 第二问、对抗 ≠ 分工、三类关键消息、Lead 收敛责任——换任何工具都成立。但「Agent Team」作为一个开箱产品，目前只有少数工具做了。

| 本节能力 | 国内工具普遍情况 | 拿不到时怎么办 |
|---|---|---|
| 成员之间直接通信 | **普遍没有** | 见下面的人肉信使方案 |
| 共享任务板 + 自主认领 | **普遍没有** | 一个 md 文件当任务板，人肉维护状态 |
| 各自独立工作副本 | 多数没有 | 课堂本来就只读 |

**人肉信使方案**（不要用「多个互不通信的 Subagent」冒充 Team）：

1. 三个会话各自调查一个竞争假设，边界照 §9.6 划。
2. A 拿到会改变 B 方向的证据时，**你手动把那段证据贴进 B 的会话**。
3. 冲突出现时，把双方证据同时贴给第三个会话做裁决。
4. 你自己维护一张任务板（一个 md 文件就够）。

这个方案教学价值其实不低：**你亲手当了一次消息总线，就知道 Team 帮你省的到底是什么。** §9.7 那句讲评（「如果禁止成员互相通信，这个任务会在哪一步变慢」），人肉版学员反而答得比自动版更准——因为慢在哪一步是他们自己扛过来的。

## 16. 官方依据与数据纪律

### 16.1 稳定原理

- Subagent 是集中协调结构：成员聚焦执行，结果回中心，由中心处理覆盖、冲突与汇总。
- Agent Team 是协作结构：成员需要共享发现、协调依赖或互相挑战，Team Lead 负责最终收敛。
- 两种结构没有固定强弱；选择依据是任务的**通信拓扑**，而不是 Agent 数量。
- 顺序调查存在锚定偏差：先探索的理论会偏置后续调查。多个独立调查者互相证伪时，活下来的理论更可能是真根因。
- Agent 数量增加会增加总 token / 计算成本、协调成本与结果合并风险；墙钟变快不等于总成本变低。
- 对话 context 与工作区隔离是两个维度。

### 16.2 产品事实只做「当天映射」

截至 2026-08-02，Claude Code 官方文档明确区分：subagent 在独立 context 中执行聚焦任务并**只回报主会话**；agent team 是独立会话组成的团队，**成员之间可直接发消息**并共享任务列表。三条对备课影响最大、必须记在案的事实：

1. **Agent Team 是实验特性，默认关闭。** 不显式打开，session 启动时不建 team、不写 team 目录，Claude 既不会 spawn 也不会提议 teammate——**不报错，静默无效**。
2. **Claude 有时会用 subagent 代替 team，而两者在同一个面板里外观相同**——只看面板确认不了。
3. **Agent Team 不给成员做工作副本隔离**，官方处方是划分文件所有权；而 subagent 可以各自跑在独立工作副本里。

参考：

- [Orchestrate teams of Claude Code sessions](https://code.claude.com/docs/en/agent-teams)
- [Create custom subagents](https://code.claude.com/docs/en/sub-agents)
- [Run agents in parallel](https://code.claude.com/docs/en/agents)

这些页面用来验证「为什么要做能力预检」，不用于在 deck 上背产品参数。若课堂改用其他工具，老师必须换成该工具的官方文档并重新实测。

### 16.3 数据纪律

- Deck 只保留跨工具成立的判断框架；具体按钮、开关字面量、并发上限、上下文继承方式放老师备注。
- **模型选型只讲档位和判断线**，不出现模型名、价格、版本号。
- 实验用来观察信息流和协调方式，不包装成模型跑分或普遍性能结论。
- 任何「更快」「更省」「更可靠」的结论都必须说明比较对象和指标。

## 17. 待确认项

1. **课堂主工具**：本节对工具的要求比 L7 硬得多——必须能真实展示成员通信和共享任务板。若主工具不支持，采用「主工具 + Team 预录」的组合，并把 §15 人肉信使升为主路径。
2. **统一兜底仓库**是否与 L7 复用同一个？建议复用，省掉重新熟悉的时间，但实验题必须换成竞争假设型。
3. **实验时长 23 分钟够不够？** 这是本节最大的时间风险——Agent Team 冷启动比 Subagent 慢，权限弹窗还会吃时间。彩排必须实测。
4. **国内学员占比**决定 §15 是主路径还是备选。这个数字会改变整节课的素材准备量。
5. **L8 之后还有没有 L9？** 若有，P17 保留预告；若无，改为 L1–L8 全主线收束。
6. 对外标题定为 **Agent Team：从分派到协作**？

---

## 18. 附录 A：Agent Team 的设置与生成方法

> **这一节不上 deck。** 老师备课和学员课后自查用。
>
> **口径与复核**：按 Claude Code 官方文档整理（2026-08-02 核对）。带版本号的条目会继续变——老师上课当天必须用手上那个版本重跑一遍 §12.2 的验收清单。换别的工具则整节作废。

### 18.1 前提：开关默认是关的

这是整节课的开课门槛。在设置文件的 `env` 段落里把 `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` 置为 `1`（也可以走环境变量）。

不设它：session 启动不建 team、不写 team 目录，Claude 既不会 spawn 也不会提议 teammate——**你会以为是自己指令写得不对，其实是功能没开。**

官方同时标注 agent teams 为实验特性，在会话恢复、任务协调、关停行为上有已知限制（见 §18.9）。

### 18.2 生成：只有自然语言，没有命令

```text
我在设计一个扫描代码库 TODO 注释的 CLI 工具。开三个 teammate 从不同角度探索：
一个负责 UX，一个负责技术架构，一个专门唱反调。
```

两条路径：你主动要，或者 Claude 判断该并行时提议、你确认。**Claude 不会未经批准就开人。** 早期版本需要先手动建 team 再拉人，现在不用了，session 退出自动清理。

**对抗辩论的关键句**（这是本节实验的命脉）：

```text
用户反馈 app 收到一条消息后就退出。开 5 个 teammate 各查一个假设。

让它们互相对话、试图推翻彼此的理论，像一场科学辩论。
最后把达成的共识写进结论文档。
```

去掉「让它们互相对话、试图推翻彼此」这半句，拿到的就是三份互不相干的报告。

### 18.3 指定人数和模型

```text
开 4 个 teammate 并行重构这几个模块，每个都用 <某档>。
```

⚠️ **teammate 默认不跟随 Lead 的 `/model`**。要跟随得去 `/config` 把默认 teammate 模型改成「跟随 Lead」。effort 倒是继承 Lead 的。**成员的模型在 spawn 时固定，事后 `/model` 只改 Lead。**

### 18.4 复用 subagent 定义当 teammate 角色

```text
用 security-reviewer 这个 agent type 开一个 teammate，审一下 auth 模块。
```

teammate 会用那份定义的 `tools` 和 `model`，定义正文**追加**到它的 system prompt（不是替换）。

两个例外，**恰好是最好的课堂教材**：

- 定义里的 `skills` 和 `mcpServers` 两个字段**当 teammate 跑时不生效**——teammate 按普通 session 的规则从项目/用户设置加载。**同一份文件，当 subagent 用和当 teammate 用，能力不一样。**
- 团队协作工具（发消息、任务管理）**始终可用**，哪怕 `tools` 把别的都限制死了。

> 讲法：拿 L7 写好的同一个角色文件，在两种结构里各跑一次，让学员看到能力边界不同。这比讲十遍「先做 capability check」都管用。

### 18.5 要求成员先出计划再动手

```text
开一个架构师 teammate 重构鉴权模块。动手前必须先过计划审批。
只批带测试覆盖的计划。
```

teammate 在只读计划模式下工作，出完计划交 Lead 审；驳回就带着反馈改了再交。**Lead 是自主决策的**——想影响它，得在 prompt 里给判据。

这条是 L6「计划先行」处方在 Team 结构里的实现，写进 charter 第 6 项收敛条件。

### 18.6 显示模式：决定老师投屏长什么样

| 模式 | 长什么样 | 代价 |
|---|---|---|
| 单终端（默认） | 全部挤在一个终端，方向键选成员，回车进它的 transcript | 任何终端都能跑，但**成员互通几乎看不见** |
| 分屏 | 每个成员一个独立窗格，同时看到所有人输出 | 要额外装终端复用工具；**部分常见终端根本不支持** |

本节的教学目标是让学员**看见**消息在成员之间流动。默认模式下这件事发生在你看不到的地方——课前必须确认上课那台机器能不能开分屏；不能的话，预录从「降级方案」升格为主要素材。

### 18.7 任务怎么分

Lead 建任务，成员认领，三态：待办 / 进行中 / 已完成。任务之间可以设依赖，**依赖没完成的任务认领不了**；多人同时抢同一个任务用文件锁挡住。

⚠️ **没有广播**——成员之间发消息要一个个点名，想让所有人知道一件事得发多条。这对辩论是真实的结构性摩擦：**证据不会自动扩散**。所以 charter 的通信规则必须写明「会改变别人方向的证据要抄送谁」。

### 18.8 落盘在哪——用它证明 team 真的成型了

团队名 = `session-` + 会话 ID 前 8 位。三样东西落在本地：

- 团队配置：成员名单，Lead 的类型固定标记为 team lead
- 共享任务列表：**会保留**，供恢复会话时接着用
- 每个成员一个信箱文件

**课堂用法**：实验开始后让学员打开成员名单看一眼——**名单里有多个成员 = team 成型了；只有你自己 = Claude 给了你三个 subagent。** 这是头号坑最便宜的检出方式。

⚠️ 团队配置文件存的是运行时状态，**不要手改也不要预先写好**，下次状态更新会覆盖。

### 18.9 权限与已知限制

**权限**：成员继承 Lead 的权限设置。**成员的权限弹窗全部出现在 Lead 那一个会话里**——23 分钟的实验不预批就会全花在点确认上。

安全边界（值得在课上讲半句）：成员之间发消息时，接收方被明确告知**这来自另一个 Claude 会话，不是来自你**；成员不能代你批权限，被拒的动作也不能转手给别的成员绕过去。这是「多 Agent 不转移最终验收责任」在权限层的同一件事。

**规模**：没有硬上限。官方建议 **3–5 人**起步，每人 5–6 个任务，并明说「三个专注的成员常常胜过五个散的」。课堂固定 3 个成员（Lead 不计）。

**已知限制**，每一条都对应一个课堂风险：

| 限制 | 课堂后果 |
|---|---|
| 单终端模式的成员**不支持恢复会话** | 谁的会话崩了成员救不回来，只能重开。要有重开预案 |
| 任务状态会滞后（成员忘了标完成） | 卡住依赖任务。**这恰好是 §6.7「计时终点不是成员都标 done」的产品级证据** |
| 关停慢（要跑完当前动作） | 下课收尾留 buffer |
| **一个 session 一个 team** | 不能跨会话共享，也开不了第二个队 |
| **不能嵌套**：成员不能再开成员 | 与 subagent 默认可嵌套形成对比 |
| Lead 固定，不能换人 | charter 里的 Lead 角色绑死，不能中途移交 |

### 18.10 头号坑：Claude 会用 Subagent 冒充 Team

官方原文明说：Claude 有时会直接用 subagent 而不建 team，**而且两者在同一个面板里外观相同，光看面板确认不了**。

这是本节实验静默降级的主要路径——全班以为在跑 Team，实际跑的是三个互不通信的 subagent，然后 §9.7 那句讲评就问不出任何东西，因为本来就没通信过。

处方两条：**指令里明确要求 agent team**；开跑后用 §18.8 的成员名单验一次。

> 别浪费这个坑——它是本节最好的即兴教材。当场问一句：**「你怎么知道你现在跑的是 Team 不是三个 Subagent？」** 答不上来，就等于承认自己在假设协作能力。

### 18.11 学员课后自查清单（作业附页）

```markdown
## 开 Team 之前
- [ ] 开关开了吗？（不开就没有 team，且不会报错提示）
- [ ] 我怎么确认成型的：看了成员名单 / 让两个成员互发了消息 / 都没有
- [ ] 显示模式：我看得见成员之间的消息吗？
- [ ] 常用只读操作预批了吗？
- [ ] 成员模型跟随 Lead 了吗？（默认不跟随，且 spawn 后改不了）

## 设计对抗的时候
- [ ] 我设计的是对抗还是分工？（按模块切 = 分工）
- [ ] 谁负责唱反调？
- [ ] 这件事有没有廉价判据？（有 → 别辩论，去跑那个判据）
- [ ] 通信规则里写明「什么证据要抄送谁」了吗？（没有广播）

## 收口
- [ ] 冲突是靠证据裁决的，还是靠投票 / 默认一致？
- [ ] 排除项有反证，还是只写了「没查到」？
- [ ] 我的验收动作来自成员自述之外吗？
```
