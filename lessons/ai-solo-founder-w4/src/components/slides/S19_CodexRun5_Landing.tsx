import CodexRun from '../CodexRun';

export default function S19_CodexRun5_Landing() {
	return (
		<CodexRun
			no="⑧"
			tag="做出那个网页"
			minutes="12 min"
			title="最后才做网页 —— 前面的东西它全用得上"
			sub="注意我一句都没描述长什么样，也没重写一遍活动介绍 —— 那些前面全定完了。网页放最后做，是因为它只是个架子，内容早就有了。"
			prompt={`读 event-sot.md 和 design-system.md，做一个活动 landing page。

技术要求：
- 单个 index.html，内联 CSS，不引任何外部资源（CDN / 字体 / 图库都不要）
- 手机优先，桌面自适应
- 严格用 design-system.md 的色板、字号阶梯、间距、圆角

页面结构：
1. 首屏：一句话说清为谁解决什么 + 一个证据 + 一个报名按钮
2. 这场活动会发生什么（按 SoT 的 format 段，几个环节列出来）
3. 适合谁来（按 audience 段，写成"如果你正在…"）
4. 时间 / 地点 / 人数 —— SoT 里是 TBD 的就写"待定"，不要编
5. 报名区：再放一次按钮
6. 页脚：主办方 + 联系方式占位

文案规则：
- 禁用词表在 design-system.md 语气段里，严格遵守
- 每一句话把活动名换掉后应该变得不成立
- 不承诺任何结果（不写"保证学到""一定认识到人脉"）

最后告诉我：哪些内容我必须自己补（TBD 的、需要真实数据的），列一个清单。`}
			accept={[
				'颜色字体跟 DS 一致 —— 不一致说明它没读 DS',
				'TBD 的地方老实写"待定"，没有编日期和场地',
				'首屏那句话换个活动名就不成立',
				'它主动列出了"你必须自己补"的清单',
			]}
			fallback="生成的页面样式崩了，就让它「只用最基础的 CSS 重做一版，先保证结构对」。结构对了再谈好看——今天要的是链路跑通。"
		/>
	);
}
