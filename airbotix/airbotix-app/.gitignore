node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local

# editor / OS
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# logs
*.log
logs

# tests
coverage

# build artifacts (tsc -b composite project leftovers). The vite-config project
# is meant to emit to node_modules/.cache (see tsconfig.node.json outDir), but a
# stray tsc run can still drop these in the repo root — ignore them explicitly so
# the compiled config never shadows / gets committed alongside vite.config.ts.
*.tsbuildinfo
/vite.config.js
/vite.config.d.ts

# build-time public env (kept local; CI injects via workflow env)
.env.production

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Vendored game engines (Phaser 2D + three.js 3D) — materialized from the `phaser`
# and `three` npm deps by the vendor-engines Vite plugin (vite.config.ts, runs on
# every dev/build), served from our own origin (no CDN). Tracked via the
# package.json version pins, not as binaries.
/public/vendor/phaser-*.min.js
/public/vendor/phaser-*.d.ts
/public/vendor/three-*.global.js
.claude/worktrees/
