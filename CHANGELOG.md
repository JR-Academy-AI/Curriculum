# Changelog

## 2026-08-23

- **deck 上的绝对分钟数全部清掉，37 处。** 讲师指出「硬停 52 分钟这种不要出现」—— 对的，而且不只是观感：**① 它把讲师架上被公开计时的位置**（屏幕写 52、实际讲到 58，全场都知道你落后 6 分钟，压力转移到讲师身上、学员注意力从内容转到进度）· **② 上课晚开始 5 分钟全 deck 当场失效**（绝对刻度只要一个环节移位就全错）· **③ P05 尤其不能有**（那页整个设计是 90 秒沉默，屏幕上挂个钟只会让人盯钟不动脑）。
  - **清的范围**：25 个 `PageHead` 的 `time=` · P08 / P19 两个手写页眉里的 · 7 个动作页的「硬停 84 min」。`PageHead.time` 改成可选并标废弃，注释写明不要再传。
  - **动作页改成给时长，不是给刻度**：「硬停 84 min」→「**限时 10 分钟**」，7 页分别是 2/3/4/5/3/6/10 分钟。条上的字从「硬停」（讲师术语）改成「限时」。写进蓝图的理由：**学员需要知道「我有多久」，不需要知道「现在是第几分钟」。**
  - **阶段徽章（讲 / 做 / 演 / 写）保留** —— 它告诉学员「现在该听还是该动手」，是有用信号，不是进度条。
  - **规则写进蓝图 `§11.1` 当铁律**（含三条理由），并在 `deck.tsx` 文件头和两个 `stopAt` 的 JSDoc 上都留了禁令，防止后面手滑加回去。
  - **讲稿 `§0` 清单加了一条**：**计时靠你，不靠 deck** —— 时间表要么摆在面前要么设闹钟，关键三个是 **32 分钟（图必须上屏）· 74 分钟（必须切 P22）· 90 分钟（收第 7 题）**。
  - ⚠️ **L9 的 deck 是同样的做法**（`PageHead` 逐页传 `time="26–31 min"`）。本次只改 L10，**L9 没动**，要不要一起清等讲师定。
- **L10 讲稿加了 14 个比方 + `§K` 比方索引**（1213 → 1380 行）。**L8 / L9 的讲稿里比方是 0 处** —— 那两节教的是动作（跑这段 prompt、建这道门），照做就行，不用打比方。**L10 教的是「信息在谁那里」，这东西没有形状，学员点头不代表接住了**，所以这一节第一次需要它。
  - **两条路子，看房间选**：🫱 生活路子 11 个（身体动作 / 日常场景）· 🔧 技术路子 3 个（一屋子工程师时落得更快）。**三个最难的概念各备两条，明写「不要同一个概念两条都上」** —— 都上学员会开始比较两个比方，而不是想那个概念。
  - **挂在教学难点上的，不是段子**：教游泳（§A，给讲师自己的：为什么你今天大部分时间在池边站着）· 填错收货地址（P09，**同一个错快递员你不怪、模型你就怪**，三句能力归因当场失效）· 地图和路（P11）· **注释写 what 还是 why**（P12 🔧，「写 what 的没人看因为代码本身就写着，why 是唯一不在代码里的东西，**隐藏区就是 why**」）/ 交接班的纸条（P12 🫱）· 打车四种说法（P14，「**说到第三个红绿灯右转，你等于在替他开车**」）· 装修师傅（P15，「不需要给他你家的房产证」）· **新人照着 README 走**（P16 🔧，「那一步你看不见他看得见，**AI 就是那个每次都是第一次的新人**」）/ 后视镜（P16 🫱）· ⭐**看病先说结论**（P17，「你花挂号费买的是他的判断，**结果判断你自己做完了，他只是帮你签了个字**」）· ⭐**DNS 的 TTL**（P19 🔧，机制层：「DNS 假定缓存一定会过期所以强制它回来问，**你的 CLAUDE.md 没有 TTL，所以它永远不刷新**」）+ ⭐**药箱**（P19 🫱，感官层：「**过期的药盒子看着一模一样**，不变色不响不提醒」）· 带不带伞（P21）· 面试里不说话的停顿（§C.4，给讲师）。
  - **`§K` 写了用法四条**，关键两条：**先讲正文看脸，有人皱眉再补**（比方是补丁不是主线，每个都上这节课会变成脱口秀）；**不许自己现场编新比方**（举错了学员会顺着错的往下推，你要花三倍时间往回拉）。⭐ 的默认要讲：看病必讲，退化那一对至少一个，**时间够就 TTL 在前药箱在后** —— 机制铺完再落到感官上最狠。
  - **过程中修掉两个自己造的伤**：① 后视镜和看病最初插进了 P16 口播**中间**，把引用块截断，导致「这条你其实早就学过」变成比方的一部分 → 两条都挪到各自段落末尾，看病顺势搬到 P17（那一页讲的就是「你一开口把答案说了」，本来就是它的家）；② 挪的时候删除模式没匹配上，后视镜**重复了一份** → 已删，并复验 `🫱 11 / 🔧 3 / §K 表 14 条` 三处计数一致。
- **去掉「下周二」这个编造的具体日期，全局 23 处。** 这个说法是我在设计成败判据时自己编的，不是任何真实排课日 —— 我当时想找一个比「回去以后」更有杀伤力的说法，就挑了个具体的日子。它有三个问题：**① 可能对不上**（这节课不是周一上的话，学员当场会算出来）；**② 学员会问「为什么是周二」而没有答案**，违反本系列自己的数据纪律（`§19.2` 不编造）；**③ 它已经出现 23 次**，从 P01 完成标准、P11、P22 一直到评分表「收口」维度和附录 B 讲评词，**一个编的东西重复 23 次就会被当成框架的一部分**。全部换成不绑日期的「回去做下一个真实任务时 / 一段你回去真会用的话 / 你回去之后的动作」，deck / 蓝图 / 讲稿 / `lessons.html` 四处同步，脚本复验一处不剩。顺带记一笔当初的不一致：蓝图 `§12` 那句讲评词写的是「这段话你**明天**真会粘贴吗」，跟「周二」本来就没对齐 —— 那句保留，因为课堂上说「明天」是字面为真，不是编造。

## 2026-08-22

- **L10 讲稿 `RUNSHEET.md` 写完：1213 行 / 54 节**，蓝图 `§18` 要求的 10 项全部覆盖（脚本验过）。`HANDOUT.md` 仍待写。
  - **第一幕每页都标了「🔇 闭嘴点」—— 精确到念完哪句话之后不许再出声。** 这是这节课和前九节最大的操作差别：**讲师的本能是全课最大的风险**，所以 §B 开头就写死「0 到 3 分钟之外你只问、只演示，不讲解，连续说话不超过 3 分钟一段」，并在 P03/P04/P05/P06 各标一个闭嘴点。P04、P06 还各加了一条**「忍住不纠正」**的现场提示（学员会把问题二写成技术细节、把问题四写成对 AI 能力的猜测，这两个错分别留到 P12 和 P18 用**他们自己的答案**当反例，比当场纠正有效十倍）。
  - **`§C` 第三问的 90 秒做成了秒级脚本**：0–20s 站讲台侧面**不看学员屏幕**（盯着看会被理解成在等答案）· 20–45s 有人抬头只点头 · 45–60s 教室出现笑声**让它发生**（那是压力的正常释放，制止和加入都不行）· 60–80s 有人问出声**只把原问题一字不改重念一遍** · 90s 只说「写不出来的举手」并**把人数写在纸上**。三句追问给了逐字答案，其中「能举个例子吗」只答 **「不能。举了例子这一问就没了。」** 收尾三句台词顺序不能改，**第三步最容易破功**（你会很想说「因为其实……」，而 P07 就是那个「因为」）。
  - **`§D` P07 的五种走向各配台词**，比蓝图的三级降级多出一种：**走向三「它说的三条里有错的」**。这个走向讲好了比抓到更深 —— 「它说错了这件事本身也是有用的，因为它告诉我：**这段代码会让一个认真读它的人产生这个误解**，那说明这里缺一条注释或缺一份文档。**盲区这一格给你的不一定是真相，有时候是别人会怎么误解你。**」另外把「预录必须是第三个模块」和「必须明说是预录」列成两条硬规矩，理由写在旁边：**这节课整节都在讲「你不说它就编」，讲师自己在这里含糊一下，全课可信度归零。**
  - **`§E` 埋点与回收台账**：三个「先埋不点」的东西（P13 那笔账 → P19 回收；P10「一会儿有一条你们没见过的」→ P19 末回收；P05 举手人数 → P24 优先抽他们 / §D.5 当替代证据），**并写了一句自查：翻到 P19 之前先默数这三个埋点埋了没有，少埋一个 P19 就少一层。**
  - **`§F` 止争句的使用时机**：争议不会等到 P21 才发生，给了四条触发条件（有人问「只有一半算哪一格」/ 问「两格是不是重叠」/ 两个学员开始互相争 / **你自己也说不清**），动作是三步 20 秒 —— 翻到 P21 念、补一句代价对比、翻回原页。明写**不许现场即兴回答边界问题**：答得再好都会引出下一个，这条路没有底。
  - **`§G` 讲评四种典型回答的追问方式**（只问依据不判对错）：对「四段语气一样」问「AI 读完会做四件不同性质的事吗」· 对「盲区那段先给了结论」问「你在这一格已经给了答案，它还能告诉你什么」· 对「抽象复读」只问 **「这段话你明天真会粘贴吗」** · 对「有一格标了空的」**要表扬**（空着说明还没查，不说明它不存在）。**优先抽谁给了顺序**：第三问填不出来的人排第一，写得好的排最后 —— 因为**填不出来的原因才是那一格真正的内容**。
  - **`§H` 三级砍法 + 四样不许砍**（四个问题的动手 / P07 演示 / P19 退化反转 / P22 写四问的 10 分钟）；三级砍完还超时的兜底路径也写了（那说明 P07 严重超时，直接从 P10 跳 P11，把 P12–P15 压成 8 分钟）。铁律收尾：**74 分钟必须切到 P22，无论前面讲到哪，硬产物比讲完更重要。**
  - **`§I` 助教巡场只查两件事**（四格有没有留空 / 四问是不是抽象复读），并明确 **P05 第三问留空是正常的、不许干预**，其余时段留空才要走过去说一句。`§J` 收作业写了四项各判什么，其中「三个任务的四格图**没有那一行『事后发现哪一格填错了』的不算完成**」「『我不知道』清单**全标一种的打回**」。
  - **`附录 A` 五个空**（四格定名与判定 / 你自己最常掉进哪一格 / P07 项目与三个模块 / 你自己 CLAUDE.md 的过期条数 / 一次你判断错的协作记录）。其中 A.4 附了一句理由：**你不报自己的数字，学员报数字的意愿会低一半。**
  - **`附录 B` 做砸了对照**：编了一份 4/12 分的完整课堂产出（盲区空着 / 隐藏区写成事实 / 未知区是对 AI 的猜测 / 四条指令都是「请你……」/ 审计跳过 / 边界只写「公司代码不能外传」/ 四问是 deck 原样抄一遍），逐条标了砸在哪和对应扣哪一维，并给了讲评这一份时说的话：**「最关键的是这两行 —— 盲区空着，和四问是抽象复读。这两条都是 0 分，不是因为我严，是因为这两条一错，你下周二的动作就跟没来一样。」**
  - **顺带把 deck 三处时间徽章对齐到讲稿的块内细分**：P01 `0–3` → `2–3`（P00 占 0–2）· P26 `88–90` → `88–89` · P27 `88–90` → `89–90`。脚本复验：28 页徽章起点与讲稿逐页时间全部一致（P00 封面无徽章，与 L9 惯例相同），讲稿时间单调且终点正好 90。
- **L10 deck 开发完成：28 页 P00–P27**（React 19 + Vite + framer-motion · 1600×900 SlideEngine），接入 `deploy.yml`（Build + Assemble 两处）并登记 `lessons.html`。`RUNSHEET.md` / `HANDOUT.md` 仍待写。
  - **引擎逐字拷自 `lessons/_template`**（`SlideEngine.tsx` / `ui.tsx` / `CameraBubble.tsx` / `theme.ts` / `main.tsx` 一行没动，按 `_template/README.md` 的规矩「引擎只拷不重写」）。`deck.tsx` 从 L9 拷来改造：`DEBT` 三债换成 **`QUAD` 四格 + `MOVES` 四条搬运**，`PageHead` 去掉硬编码的 `/ 15` 改成自由 `mark`，阶段徽章从 讲/看/做/讲评 换成 **讲/做/演/写**（对齐蓝图 §11.2 逐页表）。
  - **`QUAD` 里 hidden 用紫色是刻意的**：L9 的知识债也是紫色，而「隐藏区 = 知识债」正是本节承接 L9 的那句话（§1.1）。**学员换课不换色。**
  - **新增两个本节专用构件**：`AskBoard`（第一幕 P02–P06 的四问页，同版式 + 同中性色 + 顺序编号，**不分组不加分类标题不排 2×2**）和 `QuadGrid`（2×2 四格图，**P08 之前一次都不许出现**，`cells` 可注入学员刚填的那四问，让揭示变成「原来我填的是这个」而不是「老师给了我一个新框架」）。
  - **铁律用脚本验过，不靠肉眼**：剥掉 `//` 注释后检查第一幕八页的**渲染内容**，确认没有「象限」二字、没有 `QUAD`/`QuadGrid`/`QuadChip`/`MoveRow` 任何四格组件、**没有任何 `gridTemplateColumns`**（连布局层面都不给 2×2 的暗示）。「象限」在渲染内容里的**首现页 = P08**，和图同时上屏。封面按 review 结论叫 **「你以为它知道」**，`index.html` 的 `<title>` 也一并不用真名（比蓝图的许可口径更严一档，省掉「课前检查标签页有没有暴露在投屏区」这个动作）。
  - **28 页逐页 headless Chrome 走查（1600×900），抓到并修掉两处真实溢出**：**① P27 Exit ticket** 八行装不下 900px，副标题被题目列表压住、第 8 题被底部黑条盖掉一半 → 压行（gap 8→6、行内边距 10→7、序号 34→30、正文 22→21）；**② P12** 两个白卡的**第四条被裁掉一半** → 压正文（行高 1.75→1.5、字号 23→21）+ Verdict 去掉 label，**四条一条没删**。其余 26 页无裁切、无出界、无 flex 压盖。截图里若干深色块偏灰是 headless 抓帧早于 framer-motion 收敛的产物，不是真问题（`initial`/`animate` 必然收敛到 opacity 1，与 L1–L9 在产线上的写法一致）。
  - **本地 `bun` 不在 PATH，用 npm 构建**（`tsc -b` 零错误 + vite build 通过）。L7/L8/L9 的 `package-lock.json` 也都是同样来的（git 未跟踪）。CI 侧仍走 `deploy.yml` 里的 `bun install && bun run build`，不受影响。
  - **另修蓝图一处残留紧张**：`§8.1` 第一步现在写明 **P11 过场页那 1 分钟从这一步里出**（原表把 44–55 的 11 分钟全分给了 §8.1 三步，P11 没有分到时间），并重申**第四条搬运不许在 P11 提前说**。
- **L10 蓝图过了一遍 Codex 无头 review，修掉 9 处（7 条真 bug + 2 条无歧义缺陷），命名口径与休息安排两项按讲师意见保留原样。** 文件从 868 行到 918 行。
  - **① 封面直接破了那条唯一不可协商的铁律。** 蓝图规定 32 分钟前不许出现「象限」二字，但 P00 封面标题就叫「四象限 AI 协作协议」，**开场第一页就把第 32 分钟那次揭示破掉了**。改成 **P00 = 「你以为它知道」**，真名首现挪到 P08 和图同时上屏；`§11.1` 补了三条：约束覆盖封面标题本身、浏览器标签和 `lessons.html` 卡片可以用真名（投屏看不见）、**但讲师不全屏时要确认标签页标题没暴露在投屏区**。
  - **② 搬运图把退化的箭头画反了。** 正文定义退化是「开放 → 隐藏」，图里印的是 `开放区 ◀── … ─── 隐藏区`，箭头从隐藏指向开放，**把本节最核心的新机制画反了**。改成 `开放区 ──▶ 隐藏区`，标注从「唯一反向的一条」改成「唯一一条流出开放区的」（前三条都是流入，这条是流出，原措辞在四条并列时容易再读反）。
  - **③ 两处 `§` 交叉引用指错地方**：`§1.5` 的「见 §14.1 铁律第 1 条」实际在 `§10.1`（§14 是作业）；`§4` 非目标的「只讲能力维度差异（§11）」实际在 `§8.2`（§11 是 slide 蓝图）。会把做 deck 和 runsheet 的人送到无关章节。
  - **④ 有一项必交产物完全没有考核。** `§3` 学习目标第 8 条和 `§3.1` 过关证据第 6 项都要求「一条数据边界声明」，但评分表五维和 Exit ticket 七题都没查它，**学员可以完全按文档规则过关却没做这件被写成必做的事**。评分表补第六维「边界」（0 分没有 / 1 分只是「公司代码不能外传」这种笼统话 / 2 分指名哪一类信息不搬 + 给出「搬形状不搬内容」的替代写法），过关线 8/10 改 **10/12**；Exit ticket 补第 8 题，改成八题至少六题正确、第 1、5、7、8 必须正确。
  - **⑤ 70 到 72 那两分钟严重超配。** 原来 2 分钟要装退化揭示、退化循环图、三句收口、止争页和四种代价对比，deck 还要在同一格塞 P19 到 P21 三页，**止争页实际没有分到时间**。重排成 **退化 5 分钟（67–72）+ 止争独立 2 分钟（72–74）**。补时来源：映射表 7→6、搬运一 13→11（粒度 5→3）、讲评 4→3、出处 2→1，**前 32 分钟一格没动**（第一幕是地基）。新表 15 行连续无缝、合计正好 90，并把「为什么这么改」写进表下，防止后面改回去。
  - **⑥ 88 到 90 两分钟装不下那份 Exit ticket**（7 道实质题、要求至少 5 题正确、3 题必须正确，答不完更收不完）。改口径：**课堂上只收第 7 题（那段开场四问，硬产物，不许带回家补），其余七题课后 24 小时内线上提交，判卷在课后。**
  - **⑦ 现场调用只有「它说的三条我全知道」一种兜底，没有断网 / 工具挂掉 / 权限被拒 / 超时无响应的任何路线**，而 P07 又被列为不许砍。这是相对 L7（离线兜底）/ L8（环境降级矩阵）/ L9（预录输出 + runsheet 恢复流程）的实质倒退。新增 `§6.4` **三级降级**：一级换备用模块重跑（仍是现场）→ 二级投**第三个模块**的预录并**必须明说是预录、不许假装现场** → 三级只讲不演，**用 P05 第三问填不出来的举手数当替代证据**（那个数字本身就证明盲区存在）。同时点明一句关键区分：**「不许排练」只约束当天要用的那个模块，不豁免运维兜底**，所以二级预录必须是第三个模块，否则排练禁令形同虚设。课前清单、`§12` 失败模式表、`§18` RUNSHEET（P07 三种走向改五种）、`§20` 最终检查同步。
  - **另外修掉两条 Codex 没归类但无歧义的缺陷**：`§5` 说第一幕 0 到 20 分钟「老师只问，不讲」，而流程表 0 到 3 分钟教学方式写「讲」，**自相矛盾** → 明写 0 到 3 分钟开场是唯一例外，P02 之后才进入只问模式；`§8.1` 第二步给 6 分钟让每个学员逐条标自己的 `CLAUDE.md`，**没有规模上限**，长文件会吃掉整个 `§8.1` → 加上限「最多标 15 条，更长的只标你印象里最常被它违反的那 15 条」。
  - **按讲师意见保留原样、没有改的**：「四条搬运」与「四条 + 一条退化」的叫法不统一、Johari 出处算「一处扩展」还是「两处」（这两条是命名口径，等讲师拍）；不排中场休息（L5 到 L9 每节都排了 5 分钟，L6 同样 90 分钟也排了，**这是本系列的明确例外**，理由是保护第 32 分钟的揭示；`§10.1` 铁律第 6 条补了「若不认这个取舍，改法是放在 44 分钟并从动手段借 5 分钟，不许放在 44 分钟之前」）；P07 不许提前跑（Codex 判为「可能是有意的，不能安全归为 bug」，讲师的教学判断，保留）。

## 2026-08-21

- **新增 L10《四象限 AI 协作协议》蓝图 v1.0（868 行）。deck / RUNSHEET / HANDOUT 尚未开工，`lessons.html` 待 deck 完成后再登记。**
  - **系列首次从机制层转向理论层。** L1 到 L9 全是机制（SoT / 交付链路 / Skills / 诊断 / Subagent / Team / 老项目），九节课给了几十条规则，而规则只在教过的场景里有效。L10 换的是层次不是话题：**前九节给你动作，第十节给你一张能自己推出动作的图。** 蓝图 `§0.1` 明写了这个转向的代价，防止后面自我欺骗：**这节课不会让学员多一项能力**（四格里三格对应的动作学员已经会做），它唯一的价值是**让学员在没教过的第十种情况下做对**。因此全节只有一条成败判据：「一个学完的人，下周二做真实任务时，动作和没学的人不一样吗」。离场状态是「思路更清楚了」即判定失败。
  - **核心不是四个格子，是四条搬运动作。** 四格（开放 / 隐藏 / 盲区 / 未知）本身是一页纸，讲完加举例 30 到 35 分钟就见底，90 分钟必然注水。撑住时长的是格子之间的搬运：**隐藏 → 开放 = 落盘（L1–L3）· 盲区 → 开放 = 先让它说你别先下结论（L6/L9）· 未知 → 盲/隐藏 = 设计实验不是设计 prompt（L7）· 开放 → 隐藏 = 退化**。第四条是本节最值钱也最原创的一页：**开放区不是你达成的状态，是你维护的状态**，而且它退化时**没有报错**。Johari 原型里没有这条（人际关系里信息不会自己过期，代码库里会），蓝图要求 deck 明写这是本课扩展。
  - **最重要的教学决定：四格图必须到第 32 分钟才第一次出现**（`§0.3` + 节奏铁律第 1 条，全课唯一不可协商）。理论课只有一种死法：图在第 8 分钟上屏，之后 80 分钟都在解释一张已在屏幕上的图，学员从「我在想」变成「我在听」再也回不来。所以顺序是反的：**0–20 分钟学员回答四个并列问题（不给任何框架）→ 20–32 分钟老师用自己那个老项目现场暴露一次盲区 → 32 分钟图出现，台词固定为「你们刚才填的东西有名字」**。连带约束写进 deck 检查：P00–P07 不许出现任何 2×2 布局、四格配色或「象限」二字，四问不许排成方阵。
  - **第三问（盲区）学员会卡住，卡住就是教学内容。** 硬性要求 90 秒沉默、不举例、不换说法（举例等于把答案给了），然后立刻切老师演示。**老师演示承认的是「无知」，比 L9 承认「遗忘」更重一档**，并且蓝图明写 **P07 那次调用不许提前跑**（排练过的惊讶是假的），同时给了「它说的三条我全知道」的兜底台词和第二模块方案，还不行就承认「今天没抓到」往下走，不许硬演。
  - **隐藏区不教「要落盘」，教「你以为你落盘了」。** 学员学过三节 SoT，这一格最容易被敷衍过去。破法有三步：用他们自己第二问的答案当反例（**真正的隐藏区是理由和约束，不是事实**，事实它能读，理由只在你脑子里，直接接 L9 的 `status == 7`）→ 现场打开自己的 `CLAUDE.md` 数过期条目（**这是退化反转页的引信，先埋不点**，老师要先报自己的数字）→ 粒度问题（意图 / 规格 / 判据 / 实现，**只给 L11 埋点，不许展开**）。
  - **硬产物是一段「你下周二真会粘贴」的开场四问**，deck 只给骨架不给范文（给范文四格就变成填空题），并配一页「为什么这么组词」原理层（四段分开写 / 有冲突以这里为准 / 先说结论不要直接改 / 提最小实验先别做 / 明确说不知道不要猜）。收口接 L5 那条判断线：**同一段话说到第三次，它就该是个 Skill**，作业第 2 项硬性要求交 `SKILL.md` + 一次调用记录。
  - **课堂规则两条写进 deck**：① 讲评**只问「你这一条属于哪一格、依据是什么」，不判对错**（一判对错学员就转向猜老师想要的答案，框架当场退化成考试题；而且学员的任务在他自己的隐藏区里，老师本来就没有判对错的信息）② 边界争议一句话止住：**格子的边界不重要，搬运动作才重要；分不清就当隐藏区处理**，因为四种错判里它的代价最小（错当开放区它会编且你不知道，错当盲区白问一轮，错当未知区白做一个实验，错当隐藏区只是啰嗦）。
  - **出处放到第 86 分钟才说**（Johari Window, Luft & Ingham 1955）。开场说「这是 1955 年的模型」，学员立刻归档成旧东西，注意力当场掉一半。先用完再标出处，并明写两处扩展：用于人机协作不是 Johari 原意、退化方向是本课新增。
  - **国内与企业环境独立一页**：隐藏区搬不过去时**搬它的形状，不搬它的内容**（「有一类客户走特殊结算路径，标记在 X 字段」而不是真实客户名）。四问本身不依赖任何特定工具，能力差距只出现在盲区（对陌生代码库能看多深）和未知区（能不能自己设计并跑实验）两格，**只影响搬运效率，不改变协议**。学员的 `CLAUDE.md` 属于个人材料，只交「过期几条」这个数字和改动对照，**不许要求投屏或上交内容**。
  - **`§0.5` 明确标出一处本蓝图无法代填的空**：四象限是讲师原创框架，本蓝图按其「用法」（四格 + 结尾单独留「只有你能回答的问题」= 隐藏区）反推骨架，四格中文定名、每格判定标准、以及**讲师自己最常掉进哪一格**（开场第一句的素材）必须讲师课前填，收进附录 A 的五个空。
  - **L11 到 L14 的排布和剂量刻意未锁**（`§0.4`）。已知判断：五个候选框架里只有四象限、委托与可验证性、系统杠杆点三个能产出「下周二会做的动作」，抽象阶梯和不可委托更像洞察而非协议，可能应并进别节。所以 L10 收口**只埋点不承诺**，deck 不许出现「下节课讲 X」。

## 2026-07-30

- 把 Vibe Coding 大师课第六节《Agent》从 130 分钟动手工作坊改版为 **90 分钟诊断课**（Rick：实操时间不够，主要讲会遇到的问题、怎么定位、怎么改）。主干重构为诊断链「① 会遇到的问题 → ② 怎么定位 → ③ 怎么改」：五条跑偏机制从中段素材升格为主干；新增 `L6P02_TodayMap`（三段地图）、`L6P10_FiveDeaths`（五条机制总览）、`L6P16_ThreeQuestions`（定位三问）、`L6P17_LookupTable`（症状 → 机制反查表，学员带走）、`L6P18_ABDemo`（A/B 预录对照，替代原课上实跑）、`L6P19_DiagnosisDrill`（10 分钟诊断单练习，替代长任务实操）、`L6P20_FixOverview`（机制 → 处方一一对应）、`L6P25_FixWriteToDisk`（落盘处方）、`L6P26_FixInterrupt`（打断，合并原两页）；删除 `L6P13_HandsOnA`（后台跑长任务）、`L6P09_ThreeBeatsOfTasking`（被处方段吸收）、原 `L6P22_ABDiagnosis`；交付单/计划先行/可执行验证三页重构为「处方」定位并按新编号重排。deck 24 页 → 28 页，蓝图升 v0.2（含 v0.1→v0.2 改版对照表），RUNSHEET 按新结构全篇重写（含分钟级节奏、逐段讲稿、救场表），同步更新 `lessons.html` 卡片（`lessons/vibe-coding-master-l6`、`lessons/VIBE_CODING_MASTER_L6_BLUEPRINT.md`、`lessons.html`）
- 新增 Vibe Coding 大师课第六节《Agent》网页版讲座 deck：24 张 React SlideEngine slide，讲清 Agent = 模型 + 工具 + 循环、循环的四拍与「它每轮重新读一遍 context 再决定」、核心立论「它没有记忆只有 context」（并把前五节所有 SoT 重新解释成 context 治理，作为系列收束页）、交任务三拍（计划 → 执行 → 验证）与铁律「它说完成了不算完成」、五条长任务跑偏机制（context 稀释 / 压缩丢细节 / 错误累积 / 目标漂移 / 进度幻觉，每条配学员认得出的症状）、该打断的三个信号与打断后怎么给新 context，以及 A/B 红灯实验（裸交 vs 任务交付单，过关标准是能指认跑偏机制）；新增 `MechPage.tsx` 作为五条机制页的共用版式；配 `VIBE_CODING_MASTER_L6_BLUEPRINT.md`（内容 SoT）+ `RUNSHEET.md`（含分钟级节奏、十段逐字讲稿、救场降级表），登记到 `lessons.html`（尚未接入 `ai-builder/outline.json`，待 bootcamp-sync；PRD.md 待补）（`lessons/vibe-coding-master-l6`、`lessons.html`）
- 新增 Vibe Coding 大师课第六、七节课程蓝图：L6《Agent —— 原理与驾驭长任务》与 L7《Agent Team —— 从一个 context 到一支队伍》。两节是「诊断 → 解法」关系：L6 诊断出 context 稀释 / 压缩丢细节 / 错误累积，L7 的 context 隔离正好治这三条；系列主线因此走完三步（L1–L5 往 context 里放对的东西 → L6 看懂 context 怎么被消耗 → L7 给 context 分家）。原 L6《从静态到动态 / Auth + Database》蓝图按 Rick 决定保持删除，内容留在 git 历史（`lessons/VIBE_CODING_MASTER_L6_BLUEPRINT.md`、`lessons/VIBE_CODING_MASTER_L7_BLUEPRINT.md`）

## 2026-07-29

- 将 CCAR-F YouTube 封面升级为固定 JR Academy 女性虚拟讲师版本，保留原 `v1` 并在发布 SoT 增加人物母图与身份一致性检查（`cca-f-cert-pack/video-ad-remotion-15s`、`cca-f-cert-pack/public/assets`）

## 2026-07-28

- 新增 CCAR-F 6 分 19 秒 YouTube 完整指南：用 12 个信息场景讲清考试结构、五大领域、16 节课程、30 项能力要求、近 480 道原创题、双模式模考、原创场景题和两周计划；补齐 Amy 配音、真实 Demo Exam 操作、配乐、字幕、逐字稿、image model 封面、联系表与 1080p 母版（`cca-f-cert-pack/video-ad-remotion-15s`、`cca-f-cert-pack/public/assets`）

## 2026-07-24

- 把 Vibe Coding 大师课 L5《Skills》deck 里所有"用本仓库 `.claude/skills/`（`talk-deck`/`xhs-poster`/其余 14 个 Skill）当教材"的例子，全部换成 Anthropic 官方文档（`code.claude.com/docs/en/skills`、`platform.claude.com/.../agent-skills/overview`）原文给出的真实例子：`L5P05_RealSkillTeardown`（summarize-changes/pdf-processing 的 frontmatter）、`L5P06_SkillMdStructure`（fix-issue + argument-hint、pdf-processing 的真实目录树）、`L5P09_MetaExample`（原"这套课的 deck 就是 talk-deck 做的"改为官方真实功能 `/run-skill-generator`——专门生成别的 Skill 的 Skill）、`L5P16_SkillLibraryGrows`（原本仓库 14 个 Skill 列表改为 Claude Code 9 个真实 bundled skill + 官方开源 `github.com/anthropics/skills` 仓库 + 插件市场）；同步重写 PRD.md 核心教学决策/数据纪律、RUNSHEET.md 全篇讲稿与 附一、`lessons.html` 卡片描述，课程内容与本课程仓库解耦（`lessons/vibe-coding-master-l5`、`lessons.html`）

- 给 Vibe Coding 大师课 L5《Skills》deck 的 `L5P06_SkillMdStructure` 把「支持文件」从笼统的"模板/脚本"拆成官方三类（Instructions 参考文档 / Code 脚本，代码不进 context 只有运行结果进 / Resources 素材模板示例），并补上通用目录树示意（不假称本仓库有真实案例）；RUNSHEET.md 同步展开三类讲法，并新增"有没有 YAML frontmatter，Level 1 token 上限是否一样"的澄清（结论：上限一样，都封顶在官方 1536 字符的 skill 列表预算里，差的是匹配精度不是 token），同步更新 PRD.md 逐页 spec 与数据纪律，并顺带把此前"career-bootcamp 等三个是旧格式"的不准确措辞改成"没写 frontmatter，退到正文首段当 description，是官方支持的合法简化写法"（`lessons/vibe-coding-master-l5`）

- 核对 Vibe Coding 大师课 L5《Skills》RUNSHEET.md 里渐进式披露/Project vs Personal Skill 的讲法与 Anthropic 官方文档（`code.claude.com/docs/en/skills`、`platform.claude.com/.../agent-skills/overview`）一致，补上官方给的精确 token 数字（Level 1 ~100 token/skill、Level 2 <5k token、Level 3 按需 0 token）与来源引用，并如实说明官方其实还有 Enterprise/Plugin 两层（今晚课程只讲对个人/小团队最常用的 Project/Personal 两层）（`lessons/vibe-coding-master-l5/RUNSHEET.md`）
- 给 Vibe Coding 大师课 L5《Skills》RUNSHEET.md 补充 Project Skill（`.claude/skills/`，随项目 git 共享）vs Global/个人 Skill（`~/.claude/skills/`，跟着用户走）的区别与设置方法，讲稿追加到 §4（拆真实 Skill）和 §6（落地前先定位置），并在附一加一条对应降级预案；只改 RUNSHEET.md 讲稿，不涉及 deck slide（`lessons/vibe-coding-master-l5/RUNSHEET.md`）

- 给 Vibe Coding 大师课 L5《Skills》deck 扩展 SKILL.md 组成格式并新增渐进式披露原理页：`L5P06_SkillMdStructure` 补上 `argument-hint`（本仓库 14 个 Skill 里 11 个真实在用）+ 支持文件说明，`L5P06b_ProgressiveDisclosure`（新增）讲三层加载模型（常驻 description / 触发时读正文 / 按需读支持文件）及 Skill 好处（可复用/一致性/高效/团队共享/可版本管理）；deck 由 22 页扩到 23 页，同步更新 `PRD.md`/`RUNSHEET.md`/`lessons.html`，并把 `L5P17b_DomesticAlternatives` 里的模型名同步更正为 Kimi K3（`lessons/vibe-coding-master-l5`、`lessons.html`）
- 给 Vibe Coding 大师课 L5《Skills》RUNSHEET.md 的「附一：现场卡住了怎么降级」从 6 条扩到 14 条，覆盖新增章节（prompt 原理页、国内替代方案）的降级预案，并按优先级给出时间不够时的取舍顺序（`lessons/vibe-coding-master-l5/RUNSHEET.md`）
- 给 Vibe Coding 大师课 L5《Skills》deck 加两页实战向内容：`L5P11b_PromptAnatomy` 逐句拆解投屏 prompt 为什么这样组词（上下文先行/产出形态先定/结构化字段拆开要/检查点前置），`L5P17b_DomesticAlternatives` 讲国内用不了 Claude Code 时的三条退而求其次路线及生态差距；deck 由 20 页扩到 22 页，同步更新 `PRD.md`/`RUNSHEET.md`/`lessons.html`（`lessons/vibe-coding-master-l5`、`lessons.html`）

## 2026-07-22

- 重做 CCAR-F 90 秒 YouTube 缩略图：改用图片模型完成图文一体设计，以陌生观众可直接理解的“Claude 架构师证书”为最大标题，并逐字校验中文与技术词（`cca-f-cert-pack/public/assets`）
- 新增 CCAR-F 90 秒 YouTube 干货型横屏视频：用原创退款场景讲解 prompt、PreToolUse 与重试的架构判断，复用真实题库和双模式模考录屏，补齐 Amy 配音、字幕、逐字稿、封面、联系表及发布规格验收（`cca-f-cert-pack/video-ad-remotion-15s`）
- 新增 Vibe Coding 大师课第五节《Skills》网页版讲座 deck：20 张 React SlideEngine slide，讲清 Skill 是什么、与一次性 prompt/rules 的区别、该不该做成 Skill 的判断线、`description` 触发命门、拆解本仓库真实 `talk-deck`/`xhs-poster` Skill，并带学员动手写一个 Skill、调用、迭代；配 `PRD.md` + `RUNSHEET.md`，登记到 `lessons.html`（尚未接入 `ai-builder/outline.json`，待 bootcamp-sync）（`lessons/vibe-coding-master-l5`、`lessons.html`）

## 2026-07-21

- 发布并绑定 CCDV-F 第 7–10 章重制 Production Release：70/70 张签名缩略图返回图片，140/140 段音频支持 `206 audio/mpeg` 分段播放，课程登记同步改为已发布（`lessons/ccdv-f-{prompt-context-engineering,security-safety,tools-mcps,exam-prep}`、`lessons.html`）
- 重做 CCDV-F 第 7–10 章完整配音：将 140 段旧版 Eleven v3 + 1.18 倍后处理替换为 Amy Multilingual v2、0.92 语速、固定 seed、上下文衔接且保留自然停顿；补齐 70 张同 Release 缩略图，并增加逐文件编码、时长、声线配置和五视口固定画布闸门（`lessons/ccdv-f-{prompt-context-engineering,security-safety,tools-mcps,exam-prep}`）
- 修复 CCDV-F Claude Code 云端固定画布 QA 在切换 Slide 后未等待新增中文字形加载的问题，避免 `settings-precedence` 标题被误判为跨视口重排（`lessons/ccdv-f-claude-code`）
- 修复 CCDV-F 第 2–6 章 Production Manifest 缺少 `thumbnailUrl` 的问题，为 77 张 Slide 接入同 Release 缩略图并增加构建文件存在性闸门（`lessons/ccdv-f-{agents-workflows,applications-integration,claude-code,eval-testing-debugging,model-selection-optimization}`）
- 修复 CCDV-F 第六课第一页权重条的双重边框与标题压线排版，并增加标题和轨道不得重叠的视觉 QA 闸门（`lessons/ccdv-f-model-selection-optimization`）

## 2026-07-20

- 修复 CCDV-F 第五课第 4 页中 429、500、529 HTTP 状态码的逐位数字朗读，并增加单段语音重生成入口 (`lessons/ccdv-f-eval-testing-debugging`)
- 配置 Classroom Deck 发布 Runner 安装 ffmpeg/ffprobe，使配音时长、编码规格与语速闸门在 CI 中可执行（`.github/workflows/publish-classroom-deck.yml`）
- 修复 CCDV-F 第一章配音过快与分段声线漂移：移除 1.18 倍速和全段静音裁剪，改用 Amy Multilingual v2、0.92 速度、固定 seed 与上下文衔接，重生成 41 段 15:34 配音并新增语速/声线配置闸门（`lessons/ccdv-f-exam-overview-pilot`）
- 发布 CCDV-F 十章 React Classroom Production Release 并绑定生产章节，补全线上登记；移除第一章页脚的本地试验标签（`lessons/ccdv-f-*`、`lessons.html`）
- 修复 CCDV-F 第四章 Claude Code 云端 QA 的字体加载竞态，在建立布局基线前等待 `document.fonts.ready`，并在失败时输出具体重排明细（`lessons/ccdv-f-claude-code`）

## 2026-07-19

- 更新 CCDV-F 第六至第十章的课程登记为 UAT Draft 已发布，保持 Production 未绑定（`lessons.html`）
- 新增 CCDV-F 第十章 Exam Prep 的 18 张内容驱动 React Slide、36 段 Amy 配音、18 张缩略图与 Classroom Bridge；完成 18 页 × 5 容器共 90 项固定 16:9 QA，作为独立 UAT Draft 发布候选，不绑定生产课程（`lessons/ccdv-f-exam-prep`）
- 新增 CCDV-F 第九章 Tools and MCPs 的 18 张内容驱动 React Slide、36 段 Amy 配音、18 张缩略图与 Classroom Bridge；完成 18 页 × 5 容器共 90 项固定 16:9 QA，作为独立 UAT Draft 发布候选，不绑定生产课程（`lessons/ccdv-f-tools-mcps`）
- 新增 CCDV-F 第八章 Security and Safety 的 16 张内容驱动 React Slide、32 段 Amy 配音、16 张缩略图与 Classroom Bridge；完成 16 页 × 5 容器共 80 项固定 16:9 QA，作为独立 UAT Draft 发布候选，不绑定生产课程（`lessons/ccdv-f-security-safety`）
- 新增 CCDV-F 第七章 Prompt and Context Engineering 的 18 张内容驱动 React Slide、36 段 Amy 配音、18 张缩略图与 Classroom Bridge；完成 18 页 × 5 容器共 90 项固定 16:9 QA，作为独立 UAT Draft 发布候选，不绑定生产课程（`lessons/ccdv-f-prompt-context-engineering`）
- 修复 CCDV-F 第六章云端 QA 的字体加载竞态，在建立首个布局基线前等待 `document.fonts.ready`，避免 CI 使用 fallback 字体后切换造成伪 reflow（`lessons/ccdv-f-model-selection-optimization`）
- 新增 CCDV-F 第六章 Model Selection and Optimization 的 18 张内容驱动 React Slide、38 段 Amy 配音、18 张缩略图与 Classroom Bridge；完成 18 页 × 5 容器共 90 项固定 16:9 QA，作为独立 UAT Draft 发布候选，不绑定生产课程（`lessons/ccdv-f-model-selection-optimization`）
- 新增 CCDV-F 第五章 Eval, Testing, and Debugging 的 13 张内容驱动 React Slide、28 段 Amy 配音、13 张缩略图与 Classroom Bridge；完成 13 页 × 5 容器共 65 项固定 16:9 QA，作为独立 UAT Draft 发布候选，不绑定生产课程（`lessons/ccdv-f-eval-testing-debugging`）
- 新增 CCDV-F 第四章 Claude Code 的 12 张内容驱动 React Slide、25 段 Amy 配音、12 张缩略图与 Classroom Bridge；完成 12 页 × 5 容器共 60 项固定 16:9 QA，作为独立 UAT Draft 发布候选，不绑定生产课程（`lessons/ccdv-f-claude-code`）
- 新增 CCDV-F 第三章 Applications and Integration 的 18 张内容驱动 React Slide、43 段 Amy 配音、18 张缩略图与 Classroom Bridge；完成 18 页 × 5 容器共 90 项固定 16:9 QA，作为独立 UAT Draft 发布候选，不绑定生产课程（`lessons/ccdv-f-applications-integration`）
- 新增 CCDV-F 第二章 Agents and Workflows 的 16 张内容驱动 React Slide、39 段 Amy 配音、16 张缩略图与 Classroom Bridge；完成 16 页 × 5 容器共 80 项固定 16:9 QA，作为独立 UAT Draft 发布候选，不绑定生产课程（`lessons/ccdv-f-agents-workflows`）
- 新增 CCDV-F 第一章 15 张内容驱动 React Slide、41 段 Amy 完整配音、真实 Classroom Bridge 自动翻页和逐段审核播放器，不上传、不绑定生产（`lessons/ccdv-f-exam-overview-pilot`）
- 重做 CCDV-F 第一章视觉为用户确认的 JR Course Studio 演播室语言：暖灰渐变外场、官方 Logo 顶栏、独立圆角白色教学主板、克制硬阴影和内容驱动版式；重生成 15 张缩略图，并通过 15 页 × 5 容器的 75 项固定 16:9 QA（`lessons/ccdv-f-exam-overview-pilot`）
- 修复独立 Classroom Deck CI/CD：把 MP3 上传到按 `deckId/releaseId` 隔离的 UAT/Production 专用音频桶，上传后校验对象数量与 206 Range，并让 narration、音频、缩略图和发布脚本变更都能触发 changed-deck 工作流（`.github/workflows/publish-classroom-deck.yml`）
- 修复 Classroom 发布角色无 `s3:ListBucket` 时的音频发布校验，改为逐个对象验证 `Content-Type` 与不可变缓存头，保持最小权限部署（`.github/workflows/publish-classroom-deck.yml`）

## 2026-07-17

- 更新 CCDV-F 第一张 Classroom Deck 的 UAT 音频地址、发布工作流和 Release Candidate 登记（`lessons/ccdv-f-exam-overview-pilot`）
